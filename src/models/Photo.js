/**
 * Created by Roy on 6/9/2017.
 */
import {Model} from 'backbone';
/**
 * Model for every photo in the collection
 *
 * @constructor
 */
const Photo = Model.extend({});

export default Photo;
